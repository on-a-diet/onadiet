# onadiet needs no secrets and makes no network calls by default.
# This file documents optional environment overrides (none are required):
#
# ONADIET_GHOSTSCRIPT=/path/to/gs   # optional AGPL adapter for the keto/crash PDF path (never bundled)
